<UserControl x:Class="ControlPenales.DatosEgresoCeresoView"
           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             xmlns:Views="clr-namespace:ControlPenales"
             Height="auto" Width="auto">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>

        <Label Grid.Column="0" Grid.Row="0">Fecha</Label>
        <Label Grid.Column="1" Grid.Row="0" >No. de Oficio de Salida</Label>

        <DatePicker Grid.Column="0" Grid.Row="1" Controls:TextboxHelper.Watermark="Selecciona una fecha" />
        <TextBox Grid.Column="1" Grid.Row="1" ></TextBox>

        <Label Grid.Column="0" Grid.Row="2">Autoridad que da Salida</Label>
        <ComboBox Grid.Column="0" Grid.Row="3" Grid.ColumnSpan="2"></ComboBox>

        <Label Grid.Column="0" Grid.Row="4">Motivo de la Salida</Label>
        <ComboBox Grid.Column="0" Grid.Row="5" Grid.ColumnSpan="2"></ComboBox>

        <Button Grid.Column="0" Grid.Row="7" VerticalAlignment="Bottom" HorizontalAlignment="Left" Width="160" Height="30" Margin="0 10 0 0" Style="{DynamicResource AccentedSquareButtonStyle}">Valores Predeterminados</Button>
    </Grid>
</UserControl>
