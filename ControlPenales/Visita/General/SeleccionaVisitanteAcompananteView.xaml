<UserControl x:Class="ControlPenales.SeleccionaVisitanteAcompananteView"
           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
             xmlns:Views="clr-namespace:ControlPenales"
             Height="auto" Width="auto">
        <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition Width="120"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>
        <DataGrid Grid.Column="0" Grid.Row="0" Grid.RowSpan="3" AutoGenerateColumns="False" Style="{StaticResource MetroDataGrid}" MinHeight="200">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Apellido Paterno"></DataGridTextColumn>
                <DataGridTextColumn Header="Apellido Materno"></DataGridTextColumn>
                <DataGridTextColumn Header="Nombre"></DataGridTextColumn>
                <DataGridTextColumn Header="Edad"></DataGridTextColumn>
                <DataGridTextColumn Header="Parentesco"></DataGridTextColumn>
                <DataGridTextColumn Header="Domicilio"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!--<Button Grid.Column="1" Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Top" Width="100" Margin="2 0 2 2" 
                    Style="{DynamicResource AccentedSquareButtonStyle}" CommandParameter="seleccionar_buscar_acompanante" Command="{Binding OnClick}">Seleccionar</Button>

        <Button Grid.Column="1" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Top" Width="100" Margin="2 20 2 0" 
                    Style="{DynamicResource AccentedSquareButtonStyle}" CommandParameter="salir_buscar_acompanante" Command="{Binding OnClick}">Salir</Button>-->

    </Grid>
</UserControl>
