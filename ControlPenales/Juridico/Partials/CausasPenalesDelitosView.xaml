<UserControl x:Class="ControlPenales.CausasPenalesDelitosView"
           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             xmlns:Views="clr-namespace:ControlPenales"
             Height="auto" Width="auto">
    <Grid VerticalAlignment="Top" >
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>

        <Label Grid.Column="0" Grid.Row="0" FontWeight="Bold" Content="Delitos de la Causa Penal"></Label>
        <DataGrid Grid.Column="0" Grid.Row="1" MinHeight="100" MaxHeight="100">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Delito"></DataGridTextColumn>
                <DataGridTextColumn Header="Modalidad"></DataGridTextColumn>
                <DataGridTextColumn Header="Grave"></DataGridTextColumn>
                <DataGridTextColumn Header="Tipo"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Label Grid.Column="0" Grid.Row="2" FontWeight="Bold" Content="Delitos de la Sentencia"></Label>
        <DataGrid Grid.Column="0" Grid.Row="3" MinHeight="100" MaxHeight="100">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Delito"></DataGridTextColumn>
                <DataGridTextColumn Header="Modalidad"></DataGridTextColumn>
                <DataGridTextColumn Header="Grave"></DataGridTextColumn>
                <DataGridTextColumn Header="Tipo Delito"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
      
    </Grid>
</UserControl>
